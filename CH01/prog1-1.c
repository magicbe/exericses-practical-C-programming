#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y, total = 0;
    x = 100;
    y = 200;
    total = x + y;
    printf("x=%d, y=%d\n", x, y);
    printf("total=%d\n", total);
    // system("PAUSE");
    return 0;
}